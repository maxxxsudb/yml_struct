-- БЛОК 1: КОМАНДЫ ДЛЯ РОЛЕЙ
SELECT
    'GRANT "' || ROLE_NAME || '" TO "REVOLUTE"' || CASE WHEN IS_GRANTABLE = 'TRUE' THEN ' WITH ADMIN OPTION' ELSE '' END || ';'
FROM "SYS"."GRANTED_ROLES"
WHERE GRANTEE = 'REVOLUTE' AND GRANTEE_TYPE = 'USER';
